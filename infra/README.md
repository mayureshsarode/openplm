# infra/

This directory contains infrastructure configuration files.

Infrastructure files (Docker production configs, compose overrides, etc.)
will be added here in later milestones.

Current: M4 — placeholder only.
